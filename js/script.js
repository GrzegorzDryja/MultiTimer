/* 
    Author: Grzegorz Dryja
*/
/*
function createStartButton(){
    var start = document.querySelector("#start");
    var context = start.getContext("2d");

    context.beginPath();
    context.moveTo(0, 0);
    context.lineTo(0, 10);
    context.lineTo(10, 5);
    context.closePath();

    context.lineWidth = 1;
    context.strokeStyle = "#000000";
    context.stroke();
}

function createPauseButton(){
    var start = document.querySelector("#pause");
    var context = start.getContext("2d");

    context.beginPath();
    context.moveTo(3, 0);
    context.lineTo(3, 10);    
    context.closePath();
    context.lineWidth = 1;
    context.strokeStyle = "#000000";
    context.stroke();

    context.beginPath();
    context.moveTo(6, 0);
    context.lineTo(6, 10);    
    context.closePath();

    context.lineWidth = 1;
    context.strokeStyle = "#000000";
    context.stroke();
}

createStartButton();
createPauseButton();
*/